#include "stdafx.h"

#include "ImageFileHandler.h"

bool CImageFileHandler::LoadBMP(LPCTSTR szFilePath, long& cx, long& cy, long& step, BYTE*& p)
{
	return true;
}

bool CImageFileHandler::SaveBMP(LPCTSTR szFilePath, long cx, long cy, long step, BYTE* p)
{
	return true;
}
